import React, { useState } from "react";

function ComplistCollection({ currentUser }) {
  console.log(currentUser)
  const [complists, setComplists] = useState([])
    return(
      <>
      chicken
      </>  
    );
}


export default ComplistCollection

